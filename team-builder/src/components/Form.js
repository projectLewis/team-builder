import React from "react";

const Form = () => {
  return (
    <form>
      <fieldset>
        <legend>Form</legend>
        <label>Add Team Member</label>
        <input type="text" />
      </fieldset>
    </form>
  );
};

export default Form;
